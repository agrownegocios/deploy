apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  selector:
    matchLabels:
      app: frontend
  replicas: 1
  template:
    metadata:
      labels:
        app: "frontend"
    spec:
      containers:
        - name: frontend-container
          image: agrownegocios/frontend-homologacao:v290
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "0.3"
              memory: 50M
      nodeSelector:
        disktype: node-homologacao-frontend
      imagePullSecrets:
        - name: regcred
