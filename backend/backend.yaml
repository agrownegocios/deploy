apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: backend
spec:
  selector:
    matchLabels:
      app: backend
  replicas: 1
  template:
    metadata:
      labels:
        app: "backend"
    spec:
      containers:
        - name: backend-container
          image: agrownegocios/backend:v280.02
          resources:
            requests:
              cpu: 10m
              memory: 20Mi 
            limits:
              cpu: 20m
              memory: 25Mi
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: regcred
