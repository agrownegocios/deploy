apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-hosts
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt"
spec:
  tls:
  - hosts:
    - "app.agrownegocios.com.br"
    secretName: agrownegocios-tls
  - hosts:
    - "api.agrownegocios.com.br"
    secretName: backend-tls
  rules:
  - host: "www.agrownegocios.dev.br"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: svc-frontend
            port:
              number: 80
  - host: "backend.agrownegocios.dev.br"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: svc-backend
            port:
              number: 8080