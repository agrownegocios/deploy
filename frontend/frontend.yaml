apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  selector:
    matchLabels:
      app: frontend
  replicas: 1
  template:
    metadata:
      labels:
        app: "frontend"
    spec:
      containers:
        - name: frontend-container
          image: agrownegocios/frontend:v282.02
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "2Gi"
              cpu: "1000m"
      nodeSelector:
        name: ip-172-31-6-135.us-east-2.compute.internal
      imagePullSecrets:
        - name: regcred
